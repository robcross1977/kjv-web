import { Mastra } from "@mastra/core";

/**
 * Mastra configuration for Bible search functionality
 * Provides AI-powered semantic search capabilities for biblical content
 */
export const mastra = new Mastra({
  // Configuration will be expanded as we add agents and tools
});

export default mastra;
